<h3>Bienvenido administrador {{ user.displayName}} </h3>
    <div class="row">
        <div class="col s12">
            <div class="center center-aling">
                <div class="col s12">
                    <a href="/typedevices/add" class="waves-effect waves-light btn-small large"><i class="fa fa-plus right"></i>Agregar tipo de dispositivo</a>
                </div>
            </div>
        </div>
    </div>

    {{#if typedevices}}
        <p>Mis Tipos de dispositivos.</p>

        <!--only desktop-->
        

        <table  class="display striped highlight" id="devices_table" name="devices_table">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Imagen</th>
                </tr>
                <tr id="filter_row">
                    <th>
                        <input type="text" placeholder="Filtrar name">
                    </th>
                    <th>
                        <input type="text" placeholder="Filtrar image">
                    </th>
                </tr>
            </thead>
            <tbody id="tbody">
                {{#each typedevices}}
                    <tr>
                        <td>{{name}}</td>
                        <td>{{image}}</td>
                        <td>
                            <a href="/typedevices/edit/{{_id}}" class="btn btn-float">
                                <i class="fas fa-edit"></i>
                            </a>
                            <form action="/typedevices/delete/{{_id}}" method="POST" id="delete-form">
                                <input type="hidden" name="_method" value="DELETE">
                                <button type="submit" class="btn red">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                {{/each}}
            </tbody>
            <tfoot>
                <tr>
                    <th>Nombre</th>
                    <th>Imagen</th>
                </tr>
            </tfoot>
        </table>
         <div id="pagination-container"></div>
    {{else}}
        <p>Tu no has registrado ningún tipo de dispositivo.</p>
    {{/if}}